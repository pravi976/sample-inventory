id: INVENTORY_REST_TC010_RESERVE
name: Reserve inventory REST full regression
team: inventory-team
microservice: inventory-service
service_type: rest
tags: [gate2, full, inventory-service, rest, test, release]
branches: [test, release]
severity: high
ignore_json_paths:
  - $.headers
  - $.body.expiresAt
rest:
  method: POST
  base_url: http://localhost:8082
  endpoint: /api/inventory/reservations
  headers:
    Content-Type: application/json
  expected_status: 201
  timeout_seconds: 5
